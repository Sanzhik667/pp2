#1
"""import math
def degrees_to_radians(degrees):
    radians = math.radians(degrees)
    return radians

# Input degrees from the user
degrees = float(input("Enter degrees: "))

# Convert degrees to radians
radians = degrees_to_radians(degrees)

# Output the result
print(f"{degrees} degrees is equal to {radians} radians.")
"""


#2


"""def trapezoid_area(base1, base2, height):
    area = 0.5 * (base1 + base2) * height
    return area

# Input the values of the bases and height from the user
base1 = float(input("Enter the length of the first base: "))
base2 = float(input("Enter the length of the second base: "))
height = float(input("Enter the height of the trapezoid: "))

# Calculate the area of the trapezoid
area = trapezoid_area(base1, base2, height)

# Output the result
print("The area of the trapezoid is:", area)"""

#3
"""import math

def regular_polygon_area(n, s):
    area = (n * s**2) / (4 * math.tan(math.pi / n))
    return area

# Input the number of sides and the length of each side from the user
n = int(input("Enter the number of sides of the regular polygon: "))
s = float(input("Enter the length of each side of the regular polygon: "))

# Calculate the area of the regular polygon
area = regular_polygon_area(n, s)

# Output the result
print("The area of the regular polygon is:", area)
"""
#4
"""def parallelogram_area(base, height):
    area = base * height
    return area

# Input the values of the base and height from the user
base = float(input("Enter the length of the base of the parallelogram: "))
height = float(input("Enter the height of the parallelogram: "))

# Calculate the area of the parallelogram
area = parallelogram_area(base, height)

# Output the result
print("The area of the parallelogram is:", area)
"""



